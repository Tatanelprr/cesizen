{% extends 'base.html.twig' %}

{% block title %}Nouvelle émotion{% endblock %}

{% block body %}
<div class="row justify-content-center">
    <div class="col-12 col-md-6">
        <a href="{{ path('app_admin_emotions') }}" class="btn btn-outline-primary btn-sm mb-4">
            <i class="bi bi-arrow-left"></i> Retour
        </a>
        <div class="card p-4">
            <h1 class="h4 fw-bold mb-4">Nouvelle émotion</h1>
            {{ form_start(form) }}
                <div class="mb-3">
                    {{ form_label(form.libelle, null, {'label_attr': {'class': 'form-label fw-semibold'}}) }}
                    {{ form_widget(form.libelle, {'attr': {'class': 'form-control'}}) }}
                </div>
                <div class="mb-3">
                    {{ form_label(form.category, null, {'label_attr': {'class': 'form-label fw-semibold'}}) }}
                    {{ form_widget(form.category, {'attr': {'class': 'form-select'}}) }}
                </div>
                <div class="mb-3">
                    {{ form_label(form.codeColor, null, {'label_attr': {'class': 'form-label fw-semibold'}}) }}
                    {{ form_widget(form.codeColor, {'attr': {'class': 'form-control form-control-color'}}) }}
                </div>
                <div class="mb-4">
                    {{ form_label(form.icon, null, {'label_attr': {'class': 'form-label fw-semibold'}}) }}
                    {{ form_widget(form.icon, {'attr': {'class': 'form-control', 'placeholder': '😊'}}) }}
                </div>
                <button type="submit" class="btn btn-primary w-100">Créer</button>
            {{ form_end(form) }}
        </div>
    </div>
</div>
{% endblock %}
